<section class="categories-show">

  <header class="page-header">
    <h1>
      <%= @category.name %>
      <small>(<%= pluralize @category.products.size, 'category' %>)</small>
    </h1>
  </header>

  <div class="category">
    <%= render @category.products %>
  </div>

</section>
